# ChemScan Project Handover State

## 🚀 Execution & Deployment Status
- **Current Production URL**: [https://chemscan-49989755678.us-central1.run.app](https://chemscan-49989755678.us-central1.run.app)
- **Deployment Platform**: Google Cloud Run (`us-central1`)
- **Health Check**: `/api/health` and `/api/config` are fully operational.
- **Last Deployment**: 2026-04-10 (UI Sync Fix + Schema Harmonization).

## 🛠 Tech Stack Snapshot
- **Frontend**: Vite + React + Tailwind CSS + Framer Motion.
- **Backend**: Node.js + Express (Hybrid: Vite Dev Server + API routes).
- **Database**: Firestore (Default) + Custom DB Instance.
- **RAG Engine**: Pinecone (`chemscan` index) + Gemini Text-Embedding-004.
- **AI/Search**: Gemini 1.5 Flash-Lite + Tavily Search API.
- **Payments**: Razorpay Integration (Live Mode).

## 🔑 Infrastructure Details
- **Google Cloud Project**: `gen-lang-client-0312116426`
- **Firestore DB**: `ai-studio-037afd9e-7975-495a-b35d-27afa336d0de`
- **Pinecone Index**: `chemscan` (Region: `us-east-1` usually, verify in console).

## 🔄 Recent Critical Changes (FIXED)
### 1. Schema Standardization
We synchronized the Backend/Frontend/Export schema to avoid "invisible" question text in the UI.
- **Question Structure**:
  ```typescript
  {
    text: string;      // Standardized from 'question'
    answer: string;    // Standardized from 'correctAnswer'
    type: string;      // Standardized from 'difficulty'
    options: string[];
    source: string;
    year: string;
    targetExam: string;
    topic: string;
  }
  ```
- **Backward Compatibility**: `App.tsx` now uses fallbacks like `{q.text || q.question}` to ensure old history items remain visible.

### 2. Search Priorities
`EXAM_DOMAINS` in `server.ts` is configured to prioritize high-authority Indian prep sites (Mathongo, Aakash, LearnCBSE, etc.). Ensure domains aren't removed as they act as a RAG safety net.

## 📝 Environment Variables (Required in Cloud Run)
- `GEMINI_API_KEY`
- `TAVILY_API_KEY`
- `PINECONE_API_KEY`
- `RAZORPAY_KEY_ID`
- `RAZORPAY_KEY_SECRET`
- `RAZORPAY_WEBHOOK_SECRET`
- `NODE_ENV=production`

## 📋 Outstanding Tasks / Next Steps
1. **Monitor RAG Hit Rate**: Observe if Pinecone matches are successfully reducing API costs.
2. **User Feedback**: Verify with users if the PDF/Docx formatting reflects the new standardized schema (it should, as they were already using `text`).
3. **Analytics**: The analytics dashboard is functional but could be expanded to show "Credits Burned per Subject".

---
*Generated by Antigravity on April 10, 2026.*
